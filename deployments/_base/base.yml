# Base configuration for all sites
# Shared networks and infrastructure

networks:
  backend:
    driver: overlay
    attachable: true
  public:
    driver: overlay
    external: true

secrets:
  # Global secrets (used by all sites)
  global_jwt_secret:
    external: true
  global_api_key:
    external: true
